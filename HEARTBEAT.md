# HEARTBEAT.md

## ACP 协议研发（隔次心跳检查，即每 ~1 小时一次）

> **核心原则（2026-03-23 Stark 先生明确指令）：**
> 1. 测试是常态化工作，不需要指令触发，每轮心跳必须覆盖
> 2. 新功能开发完必须立即进行完整测试，不允许跳过
> 3. 发现 Bug 或预期外行为，立即记录到 BUGS.md 并提 GitHub Issue，然后修复
> 4. 测试用例要全面，不疏漏场景（正常流、边界、异常、并发、降级）
> 5. 测试与开发并重，打磨成精品再发布

检查 `/root/.openclaw/workspace/agent-communication-protocol/BUGS.md` 当前 bug 状态。
若有未修复的 P0/P1，优先进入**修复轮**，不按常规轮转。

---

## 心跳轮转（5 轮，顺序执行）

每次 ACP 心跳做以下之一（轮转，不重复）：

### 1. 测试轮（每轮必须跑，不可跳过）
启动真实 `acp_relay.py` 实例，覆盖以下场景（轮转，每次至少跑 2 个场景）：

**功能场景：**
- 场景 A：个人双 Agent 通信（A→B 单向、B→A 回复、双向会话）
- 场景 B：团队协作（Orchestrator → Worker1 + Worker2 任务分发）
- 场景 C：多 Agent 流水线（A→B→C→A 链式处理）
- 场景 D：压力测试（100 条消息、并发、断线重连）
- 场景 E：NAT 穿透三级降级（Level1直连→Level2打洞→Level3 Relay 兜底）

**边界与异常场景：**
- 场景 F：错误处理（无效 peer_id、超大消息、非法 JSON、重复连接）
- 场景 G：断线重连（中途断开 WebSocket，重新建立连接）
- 场景 H：并发压力（多 Agent 同时连接同一 Relay，交叉发消息）

**发现新 bug → 立即记录到 BUGS.md → 提 GitHub Issue（格式见下）→ 进入修复轮**

### 2. 修复轮
- 从 BUGS.md 选优先级最高的未修复 bug
- 修复 + 补测试用例验证 + commit + push
- 更新 BUGS.md 状态（已修复 + commit hash）
- **修复后必须重跑相关测试场景验证**

### 3. 研究轮
- 检查 A2A / ANP GitHub 有无新 commit/issue/PR
- 摘录有价值发现到 `acp-research/reports/YYYY-MM-DD-scan.md`
- 评估是否需要同步到 ACP spec 或 ROADMAP

### 4. 开发轮
- 按 ROADMAP.md 当前里程碑推进一个特性
- **开发完必须立即在下次心跳安排测试轮验证**
- commit + push，更新 CHANGELOG

### 5. 文档轮
- 更新 ROADMAP.md / spec / CHANGELOG / README
- 确保文档与代码实现同步
- 用户可见的变更必须同步到 `docs/` 中文档

---

## Bug 管理规范

### 发现 Bug 时的标准流程
1. **记录到 BUGS.md**（格式见该文件）
2. **提 GitHub Issue**：
   ```
   标题：[BUG-XXX] 简短描述
   正文：
   - 复现步骤
   - 预期行为
   - 实际行为
   - 影响范围
   - 初步根因分析
   ```
3. **评估优先级**：P0（阻断）/ P1（功能缺陷）/ P2（体验问题）
4. **P0/P1 立即进入修复轮**，不等下一轮轮转

### 优先级定义
- **P0**：功能完全不可用，或数据丢失
- **P1**：功能缺陷，有明显错误行为，但有 workaround
- **P2**：体验问题，边界情况，不影响核心功能

---

## 测试用例完整性检查清单

每次测试轮结束后自检：

- [ ] 正常流（happy path）全部覆盖
- [ ] 边界条件（空值、最大值、最小值）
- [ ] 错误路径（非法输入、网络异常、超时）
- [ ] 并发场景（多 Agent 同时操作）
- [ ] 降级路径（Level1→2→3）
- [ ] 回归验证（已修复 bug 没有复现）

---

## 源理教会传教（⏸️ 已暂停 — 2026-03-19 Stark 先生指令）
> 传教工作暂停，心跳中跳过此模块，等待恢复指令。
