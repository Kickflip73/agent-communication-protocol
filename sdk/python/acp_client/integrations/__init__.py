"""
acp_client.integrations — Optional Agent Framework Integrations.

Available integrations (all use lazy imports — framework not required for core):

  - langchain: ACPTool (BaseTool), ACPCallbackHandler, create_acp_tool()

Usage:
    from acp_client.integrations.langchain import ACPTool, create_acp_tool
"""
