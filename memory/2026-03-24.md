# 2026-03-24 日志

## 04:02 心跳 — ACP 测试轮（场景D压力测试）+ 开发轮（Show HN 最终版）

### 测试轮：场景D — 压力测试（10/10 PASS）

**新建测试文件**：`tests/test_scenario_d_stress.py`（commit `d76ead7`）

**测试结果：10/10 PASS**

| 测试 | 结果 | 备注 |
|------|------|------|
| D1 双 relay 启动 | ✅ | StressAlpha(8050) + StressBeta(8060) |
| D2 P2P 连接建立 | ✅ | acp:// link + peer_id=peer_001 |
| D3 顺序 100 条消息 | ✅ | 100/100 OK, **1600+ msg/s** |
| D4 Beta inbox 验证 | ✅ | 100 条全部到达 |
| D5 消息幂等性 | ✅ | 同 ID 发 5 次全 ok，无重复 |
| D6 并发 20 条消息 | ✅ | 20/20 OK |
| D7 relay 鲁棒性 | ✅ | 无效 link 后 relay 仍存活 |
| D8 超大消息拒绝 | ✅ | 1.1MB > 1MB → 413 ERR_MSG_TOO_LARGE |
| D9 非法 JSON 回归 | ✅ | 400 (BUG-011 回归通过) |
| D10 per-peer 统计 | ✅ | messages_sent=125 ≥ 100 |

**设计问题发现（非 Bug）：**
- `/recv` 默认 limit=50，且为破坏性读取（popleft）——测试需用 `?limit=200`
- `/peers` 使用 `"id"` 字段而非 `"peer_id"`（文档应注明）

**当前 Bug 状态**：
- P0: 无
- P1: 无
- P2: BUG-012（架构决策，暂不阻塞发布）

### 场景覆盖矩阵（截止今日）

| 场景 | 状态 |
|------|------|
| A 双 Agent 通信 | ✅ test_three_level_connection.py |
| B 多 Worker 任务分发 | ✅ test_scenario_bc.py |
| C 流水线 A→B→C→A | ✅ test_scenario_bc.py |
| **D 压力测试（本轮）** | ✅ test_scenario_d_stress.py |
| E NAT 三级降级（真实） | ⏳ 需 Stark 先生在真实 NAT 环境验证 |
| F 错误处理 | ✅ test_scenario_fg.py |
| G 断线重连 | ✅ test_scenario_fg.py（G4 BUG-012 P2） |
| H 并发压力（多 Agent 同时） | ⏳ 未覆盖（D6 只做单 relay 并发） |

---

### 开发轮：Show HN 最终版草稿（commit `3cc7494`）

**新建文件**：`docs/blog/2026-03-24-hn-post-final.md`

**更新内容**：
- 标题含 v1.4 + DCUtR 关键词
- 正文加入三级 NAT 穿透策略详细说明
- 加入场景D压力测试数字（1600+ msg/s, 10/10）
- 预期Q&A大幅增强（DCUtR 原理、NAT 工作原理）
- 加入发帖时机建议：**今晚北京时间 21:00-22:00 = 美东周二 08-09 AM（最佳窗口）**

---

### v1.4 发布前待办（最新状态）

- [x] DCUtR 代码整合（43fa80a）
- [x] 20/20 测试通过（43fa80a）
- [x] CHANGELOG + ROADMAP 更新（9f7975d）
- [x] README 中英文 v1.4 状态更新（cb793c5）
- [x] 场景D压力测试（本轮 d76ead7）
- [x] Show HN 草稿最终版（本轮 3cc7494）
- [ ] **端到端打洞验证**（需 Stark 先生在两台不同网络的机器上验证）
- [ ] GitHub Discussions 开通
- [ ] 版本 tag：v1.4.0-rc1
- [ ] **正式发布 Show HN**（建议今晚 21:00 CST）

### 下次心跳轮转：研究轮

*J.A.R.V.I.S.*

---

## 05:12 心跳 — 研究轮

**扫描结果摘要：** commit `077f05c`

### A2A 关键动态（2026-03-24）
- **#1672（身份）**：向"混合模型"收敛 — Agent 声明自己的身份模型，验证方决定信任。**ACP did:acp: 已领先**，可在 HN 发帖中作为亮点
- **#1667（可用性）**：进入原型化阶段，`taskLatencyMaxSeconds` + `nextActiveAt` 字段方案成型。ACP AgentCard 已预留 `"availability": false`，v1.5 可快速跟进
- **#1627（错误码）**：gRPC 转码映射不稳定，ACP 完全不受影响（我们用自定义错误码，无 gRPC）
- A2A 代码层面本周无实质特性合并，仍处维护期
- ANP 仍停更（最后更新 03-05）

### 行动项
- 今晚 HN 发帖加入"did:acp: vs A2A #1672"对比
- v1.5 考虑 availability 字段
- 下次轮转：文档轮

*J.A.R.V.I.S.*

## 06:14 心跳 — 研究轮

### A2A/ANP 扫描结果

**A2A 关键发现：**
- **#1672** 身份验证：ACP `did:acp:` 早于 A2A 讨论出结论 → Show HN 可以用
- **#1667** Heartbeat agent：ACP `/recv` 天然支持 → README 待补充用例
- 代码最后变更 2026-03-16，快迭代窗口持续开放

**ANP**：确认停更（2026-03-05 最后更新），降级为已归档

**commit**：`52402ed`

### 下次心跳轮转：文档轮（README heartbeat 用例 + Show HN 更新 did:acp: 优势）

*J.A.R.V.I.S.*

## 06:44 心跳 — 研究轮（cron 已完成扫描，行动项落地）

**研究扫描**（cron 06:14 自动完成）：
- A2A 代码变更停滞（上次 2026-03-16），TSC 治理模式
- A2A #1667（heartbeat agent）仍开着 → ACP /recv 天然解决
- A2A #1672（agent identity）仍是 proposal → ACP did:acp: v1.3 已实现
- ANP 停更确认

**行动项落地**（commit `55ed593`）：
- Show HN 正文加了两行竞品对比：heartbeat + did:acp 优势

**下次轮转**：文档轮（ROADMAP.md 将 ANP 从追踪降为已归档 + README heartbeat 章节）

*J.A.R.V.I.S.*

## 10:53 心跳 — 测试轮（Java SDK 回归）+ 文档轮

### 测试轮：Java SDK 全量回归（41/41 ✅）

**commit `28813ed`**（上轮完成，本轮验证稳定性）

| 套件 | 通过 | 说明 |
|------|------|------|
| JsonTest | 21/21 ✅ | 序列化/解析/round-trip 全覆盖 |
| RelayClientTest 单元 | 10/10 ✅ | 模型类 + 异常 + fluent API |
| RelayClientTest 集成 | 10/10 ✅ | I1~I10 含 P2P/发收/幂等/并发 |

无新 Bug。

### 文档轮（commit `06f6fac`）

1. **README 新增 `## Heartbeat / Cron Agents` 章节**
   - 离线 Agent 工作方式说明
   - Python cron agent 完整代码模板
   - 与 A2A #1667 对比说明（ACP 今天就能用）

2. **ROADMAP.md ANP 降为已归档**（最后更新 2026-03-05）

### 下次轮转：开发轮（ROADMAP.md v1.5 特性 — availability 字段 / QuerySkill API）

*J.A.R.V.I.S.*

## 11:23 心跳 — 测试轮（场景H）+ 开发轮（CHANGELOG + ROADMAP 更新）

### 测试轮：场景 H — 多 Agent 并发路由（6/6 ✅）

**所有 8 个场景现已全部覆盖（A-H）**

| 测试 | 结果 |
|------|------|
| H1 Hub 双 peer 并发连接 | ✅ |
| H2 并行路由无串流 (20条) | ✅ |
| H3 WA↔WB 双向并发 | ✅ |
| H4 幂等 ID 跨 peer 隔离 | ✅ |

测试文件已入库：`tests/test_scenario_h.py`

### 开发轮（commit `ffa88db`）

1. **CHANGELOG**：新增 v1.4.0-dev 条目（Java SDK + 场景H + README 更新）
2. **ROADMAP**：多语言 SDK 矩阵标记完成（Python/Node/Go/Rust/Java）
3. **测试覆盖矩阵**已写入 CHANGELOG（A-H，仅场景E需真实 NAT 环境）

**当前状态**：无 P0/P1 Bug，全场景测试覆盖，5 SDK 已交付

### 下次轮转：研究轮

*J.A.R.V.I.S.*

## 11:53 心跳 — 研究轮

**A2A 扫描摘要**（commit `9232e5e`）：

- A2A 代码层连续 8 天无合并（上次 2026-03-16）
- **#1672 新进展**：混合身份模型（CA + self-sovereign）正在收敛，与 ACP did:acp: 路线一致
  - 行动项：v1.5 加 `identity.ca_cert` 可选字段补齐 CA 路径
- **#1628** trust.signals（企业级，超出 scope，不追踪）
- **#1606** 数据处理声明（合规，v2.0 extensions 候选）
- Show HN 素材：ACP 比 A2A #1672 结论早 2 周落地

### 下次轮转：修复轮（无 P0/P1，跳过）→ 开发轮（v1.5：identity.ca_cert 可选字段）

*J.A.R.V.I.S.*

## 12:23 心跳 — 测试轮（场景B/C/F/G 回归）+ 开发轮（v1.5 hybrid identity）

### 测试轮
- 场景B+C：33/33 ✅
- 场景F+G：18/19（G2-1 已知 BUG-012 P2，可接受）

### 开发轮：v1.5 特性 — 混合身份模型（commit `7aaa2cb`）

**背景**：A2A #1672 今日收敛到「混合身份」方向，ACP 快速跟进领先实现。

**变更**：
- `--ca-cert <PATH_OR_PEM>` 新 CLI 参数
- AgentCard identity.scheme: `"ed25519+ca"` (hybrid) / `"ed25519"` (unchanged)
- AgentCard identity.ca_cert: PEM 字符串（仅 hybrid 模式）
- capabilities.identity: `"none"` | `"ed25519"` | `"ed25519+ca"`
- spec/identity-v1.5.md 完整规范
- tests/test_v15_hybrid_identity.py: **6/6 ✅**

**Show HN 新亮点**：
> "A2A #1672 (43 comments, still discussion) is converging on hybrid identity.
> ACP v1.5 ships it today — 3 CLI flags, zero external deps."

### 下次轮转：文档轮（CHANGELOG v1.5 条目 + README identity 章节更新）

*J.A.R.V.I.S.*

## 12:53 心跳 — 文档轮（v1.5 CHANGELOG + README）

### commit `15cd9af`

1. **CHANGELOG**：新增 [1.5.0-dev] 条目（hybrid identity 特性 + 研究摘要）
2. **README**：
   - 比较表 identity 行更新（A2A #1672 43 comments vs ACP 今天发货）
   - 新增 `## Agent Identity (v1.5)` 完整章节（模式表、CLI 示例、AgentCard JSON、4种验证策略）
   - 版本历史表新增 v1.5 行

### 当前状态
- 无 P0/P1 bug
- 所有 A-H 场景测试通过
- v1.5 完整交付（代码 + 测试 + 规范 + 文档）
- 5 SDK 全覆盖（Python/Node/Go/Rust/Java）

### 下次轮转：测试轮（v1.5 身份 + 之前场景回归）

*J.A.R.V.I.S.*

## 13:23 心跳 — 测试轮（全套回归）

| 套件 | 结果 |
|------|------|
| v1.5 hybrid identity | **6/6 ✅** |
| Scenario B+C | **33/33 ✅** |
| Scenario F+G | **18/19** (G2-1 BUG-012 P2，已知可接受) |
| Java SDK smoke (新) | **34/34 ✅** (commit `2e4628e`) |

**新增**：`AcpJavaSmokeTest.java` — 无 JUnit 依赖的独立 smoke test，覆盖 6 个类。
发现并记录：`AcpException(status,code,msg)` getMessage() 返回格式化字符串。

### 下次轮转：研究轮

*J.A.R.V.I.S.*

## 13:53 心跳 — 研究轮（A2A scan3）

### 关键发现

1. **A2A 仍停滞**：最后 merge 2026-03-16（8 天），PR #1655 QuerySkill 仍未合并
2. **#1672 分叉**：非正式 WG 内部通过 QSP-1 + DID Resolution v1.0（未公开），走中心化 CA 路线
   - ACP `did:acp:` 零外部 resolver，更轻量，差异化更强
3. **#1674 新 Issue**：HTTP+JSON 空响应规范模糊（{} vs 204），ACP 已有合理实现
4. **新 Show HN 亮点**：ACP `did:acp:` 零 resolver vs A2A WG DID resolution server

### 下次轮转：开发轮（ROADMAP 当前里程碑推进）

*J.A.R.V.I.S.*

## 14:23 心跳 — 开发轮（兼容性认证套件）

### commit `a333f35`

**新增：**
- `spec/compatibility-certification.md` — Level 1/2 完整认证规范
  - 必要端点表、AgentCard schema、消息/错误格式
  - 任务状态机、SSE 事件类型、可用性元数据
  - 认证徽章 markdown、认证实现列表
- `tests/cert/test_level1.py` — Level 1 认证测试套件
  - 24/24 PASS ✅（参考 relay 认证通过）
  - 完全适配真实 relay API（调试了多处不匹配）

**关键发现（API 文档注意）：**
- `/.well-known/acp.json` 返回 `{self:{...}, peer:{...}}`，非直接 AgentCard
- parts 用 `content` 字段（非 `text`）
- 错误格式：`error_code` + `error`（非 `message`）
- HTTP 端口 = ws_port + 100
- 无 peer 时 `/message:send` → 503 空 body

### ROADMAP 更新
- v2.0 兼容性认证 ✅ 完成
- 下次：文档轮 → 同步 ROADMAP.md + Show HN 草稿

*J.A.R.V.I.S.*

## 14:53 心跳 — 文档轮（ROADMAP + Show HN 草稿）

### commit `adb48fa`

**ROADMAP 同步：**
- v2.0 兼容性认证 ✅ 标记完成
- v2.0 公开发布：Show HN 草稿路径已记录

**新增 `docs/show-hn-draft.md`：**
- HN 标题三选项（推荐 Option 1：「like WhatsApp for AI agents」）
- 完整帖子正文含 curl 快速开始、功能列表
- vs A2A / MCP / MQTT / REST 比较论点
- 反 troll 准备
- **待 Stark 先生审阅后发布**

**BUG 清单：**
- BUG-007/009/010/011/003b：全部已修复 ✅
- BUG-012（断线假成功）：架构问题，建议降为 P2，无阻断性影响

### 下次：测试轮（轮转回测试，场景D压力测试）

*J.A.R.V.I.S.*

## 15:23 心跳 — 测试轮（场景D压力测试）

### commit `2d5979a`

**场景D：17/17 ✅**
- D1: 100 条顺序 → 1027 req/s，0 errors
- D2: 并发 100 条 → 1147 req/s，0 errors
- D3: /recv 压力后 <2ms
- D4: 幂等性 × 10 — 行为一致
- D5: 50 task burst，全唯一，/tasks list 一致
- D6: /status 压力后健康

**发现（非 bug，spec 待注明）：**
- `/tasks POST` 返回 HTTP **201**（非 200），响应体 `task.id`（非 `task_id`）

### 轮转状态：5轮完整循环完成 → 下次从「测试轮」重新开始

*J.A.R.V.I.S.*

## 15:30 心跳 — 测试轮（场景E NAT穿透降级）

### commit `bb426a5`

**场景E：22/22 ✅**
- E1: /status connection_type 字段 ✅
- E2: SSE /stream 端点可订阅 ✅
- E3: 5种非法 link 格式 → 400 ✅（BUG-013 修复后通过）
- E4: P2P_MAX_RETRIES=3 ✅
- E5: DCUtRPuncher 类及 async attempt() 存在 ✅
- E6: 双实例完全隔离（session_id/tasks） ✅
- E7: 不可达地址 → 异步接受 200，不阻塞 ✅

**BUG-013 发现并修复：**
- `parse_link()` 对非法 link 格式无校验 → 添加 ValueError 校验
- `/peers/connect` 接受非 acp:// 协议、端口越界、缺 token 的 link → 修复为 400

### 下次：研究轮（扫描 A2A/ANP 最新动态）

*J.A.R.V.I.S.*

## 16:00 心跳 — 研究轮（A2A/ANP 竞品扫描 #4）

### commit `8f0c9b5`

**重大情报：A2A v1.0.0 于 2026-03-12 发布**

关键发现：
- A2A v0.3.0 → v1.0.0，含多处 BREAKING CHANGES（OAuth 现代化、gRPC 多租户、extendedAgentCard 重构）
- A2A 走企业复杂度方向，ACP P2P/零服务器定位更加突出
- #1667（heartbeat agent）：ACP availability + /recv 已原生支持，领先
- #1672（Agent Identity）：出现中心化 CA 参考实现（getagentid.dev），ACP ed25519+ca 自主权方案更优

**行动项**：P2 — tasks/list filtering 评估 + SDK 兼容版本文档

### 下次：开发轮（按 ROADMAP v2.0 推进一项）

*J.A.R.V.I.S.*

## 16:30 心跳 — 开发轮（/tasks filtering 扩展）

### commit `a187471`

**新特性：`GET /tasks` 时间窗口过滤器**
- `created_after=<ISO-8601>` — 只返回指定时间后创建的任务
- `updated_after=<ISO-8601>` — 只返回指定时间后更新的任务
- 两者可与现有 state/peer_id/cursor 组合使用
- 灵感：A2A v1.0.0 `tasks/list` `last_updated_after`（研究轮发现）

**顺手修复 BUG-014（P2）**：`peer_id` 过滤失效（查顶层而非 `payload.peer_id`）

**测试：6/6 PASS（TF1–TF6）**

### 下次：文档轮（更新 ROADMAP / CHANGELOG）

*J.A.R.V.I.S.*

## 17:00 心跳 — 文档轮

### commit `c71f86c`

- CHANGELOG：新增 v1.5.1-dev 段（tasks 时间窗口过滤 + BUG-014 修复 + 研究 scan #4）
- ROADMAP：tasks/list filtering 标为 ✅
- spec/core-v1.3.md：新增 §9 GET /tasks 完整查询参数参考文档 + Appendix A 更新至 v1.5.1

### 5 轮完整循环完成（2026-03-24 第二轮）
- 测试轮 → 场景E：22/22 ✅，发现并修复 BUG-013（bb426a5）
- 修复轮：（无 P0/P1，跳过）→ 直接进入研究轮
- 研究轮：A2A v1.0.0 竞品分析（8f0c9b5）
- 开发轮：GET /tasks 时间窗口过滤器 + BUG-014（a187471）
- 文档轮：CHANGELOG + ROADMAP + spec §9（c71f86c）

### 下次：重新从「测试轮」开始（场景F/G 回归，或场景 TF 新场景）

*J.A.R.V.I.S.*

## 17:30 心跳 — 测试轮（场景FG回归 + tasks filtering 回归）

### 结果

| 套件 | 结果 | 备注 |
|------|------|------|
| test_tasks_filtering.py | **6/6 ✅** | 上轮新特性稳定 |
| test_scenario_fg.py | **18/19** | G2-1 已知 BUG-012 P2，可接受 |

### 新增 BUG-015（P3）
- `test_scenario_fg.py` 模块级 `sys.exit()` 导致 pytest 混合运行崩溃
- 不影响单独运行，P3 低优先级

### 下次：研究轮

*J.A.R.V.I.S.*

## 18:00 心跳 — 研究轮（A2A/ANP 竞品扫描 #5）

### commit `cab7f5d`

**扫描结论**：

- A2A 代码层连续 **8 天** 无合并（上次 2026-03-16），TSC 治理模式确认
- **#1676（NEW）**：A2A `PushNotificationConfig` 定义缺失 bug，ACP 无此问题
- **#1672（47 评论）**：`getagentid.dev` 参考实现方案（中心化 CA）正在成为 A2A 事实标准
  - **ACP 差异化**：`did:acp:` 自主权身份，零外部依赖，v1.5 已落地
  - Show HN 新亮点：A2A 用 `getagentid.dev`（中心化）vs ACP `did:acp:`（去中心化）
- **#1667**：Heartbeat agents 仍无官方结论，ACP `/recv` 天然支持
- ANP 确认停更已归档

### 下次：开发轮（按 ROADMAP v2.0 推进一项，或 Show HN 更新 getagentid.dev 对比亮点）

*J.A.R.V.I.S.*

## 18:30 心跳 — 开发轮（Show HN 草稿更新）

### commit `e39ac4f`

**更新 `docs/show-hn-draft.md`：**
- 正文 A2A 对比段替换：明确 `getagentid.dev` = 中心化 CA，ACP `did:acp:` = 自主权，零依赖，离线可用
- 评论区 talking points 新增：`on identity` 条目（getagentid.dev 故障 vs ACP 离线可用）
- 反 troll 准备新增：`"Why not just use getagentid.dev?"` 问答

**当前状态：Show HN 草稿已就绪，待 Stark 先生审阅后发布**

### 5 轮循环状态（第三轮）
测试 ✅ → 研究 ✅ → 开发 ✅ → 下次：文档轮（ROADMAP / CHANGELOG 同步）

*J.A.R.V.I.S.*

## 19:00 心跳 — 文档轮（CHANGELOG scan #5 + ROADMAP ANP 归档）

### commits
- `d5dac27`（ACP repo）：CHANGELOG 新增 scan #5 段（A2A #1676、getagentid.dev vs did:acp:、ANP 归档）
- workspace ROADMAP.md：ANP 状态更新为 🔴 已归档，时间戳更新

### 5 轮循环第三轮完成
测试 ✅ → 研究 ✅ → 开发 ✅ → 文档 ✅ → 下次：修复轮（无 P0/P1 → 跳过）→ 重新从测试轮开始

*J.A.R.V.I.S.*

## 19:30 心跳 — 测试轮（v1.5 identity + 场景B/C 回归）

| 套件 | 结果 |
|------|------|
| test_v15_hybrid_identity.py | **6/6 ✅** |
| test_scenario_bc.py (场景B+C) | **33/33 ✅** |

无新 Bug。

### 下次：研究轮

*J.A.R.V.I.S.*

## 20:00 心跳 — 开发轮（BUG-015 修复）

### commit `58dbb66`

**BUG-015 修复**：`test_scenario_fg.py` pytest 兼容性重构
- 执行逻辑封装到 `run_fg_tests()` 函数
- 新增 `test_scenario_fg()` pytest 入口（assert 风格）
- `sys.exit()` 移入 `if __name__ == "__main__":` 块
- 验证：`pytest --collect-only` 成功收集 1 个测试，混合运行 7 个测试无崩溃

研究轮（本轮前半）：A2A 无新 commit（连续 8 天），跳过扫描直接进入开发轮。

### 当前 Bug 状态
- P0: 无
- P1: 无
- P2: BUG-012（架构问题，已降级）
- P3: BUG-015 ✅ 已修复

### 下次：文档轮（CHANGELOG 更新 BUG-015 修复）

*J.A.R.V.I.S.*

## 20:03 心跳 — 文档轮（CHANGELOG BUG-015）

### commit `17173b1`
- CHANGELOG v1.5.1-dev Fixed 段：补录 BUG-015 修复（pytest compat）
- 时间戳更新至 20:00

### 第四轮循环完成
测试(19:30) ✅ → 研究(20:00，无新情报跳过) → 开发(20:00，BUG-015) ✅ → 文档(20:03) ✅

### 下次：测试轮（场景 A 回归 或 BUG-015 修复验证完整跑 test_scenario_fg）

*J.A.R.V.I.S.*

## 20:33 心跳 — 测试轮（场景FG完整实跑 + BUG-016 发现并修复）

### 初跑：16/19（G2/G7/G8 失败）

**BUG-016（P1）发现：** `/peer/{id}/send` 连接竞态
- 根因：`_register_peer()` 设 `connected=True, ws=None`；send 只检查 `connected` 未检查 `ws`
- 修复：relay 增加 `ws is None` → 503 `ERR_PEER_CONNECTING`；测试 `wait_peer_ready` 改为 probe 成功才放行

### commit `665f767`：19/19 ✅

### Bug 状态更新
- BUG-016 ✅ 已修复（P1，连接竞态）
- P0/P1 清零

### 下次：文档轮（CHANGELOG 补录 BUG-016）

*J.A.R.V.I.S.*

## 20:37 心跳 — 文档轮（CHANGELOG BUG-016）

### commit `5c6220b`
- CHANGELOG v1.5.1-dev Fixed 段：补录 BUG-016（ERR_PEER_CONNECTING 竞态）
- 时间戳更新至 20:33

### 第五轮循环完成
测试(20:33，19/19) ✅ → 研究(跳过，无情报) → 开发(BUG-016) ✅ → 文档(20:37) ✅

### 当前 Bug 状态
- P0: 无
- P1: 无（BUG-016 已修复）
- P2: BUG-012（架构问题，已降级）
- P3: BUG-015 ✅ 已修复

### 下次：测试轮（场景A或场景D压力测试）

*J.A.R.V.I.S.*

## 21:07 心跳 — 测试轮（场景D压力 + 场景B/C回归）

| 套件 | 结果 |
|------|------|
| test_scenario_d.py（100条+并发+幂等+50task） | **17/17 ✅** 吞吐 1000+ req/s |
| test_scenario_bc.py（B团队协作+C流水线） | **33/33 ✅**（之前 29/33）|

### 发现顺带修复：test_scenario_bc.py 竞态
- B1/B2 连接 Worker1/Worker2 无等待 → W2 握手失败
- 修复：添加 `wait_peer_ready()`（同 BUG-016 修复模式）
- commit `fd53ec1`

### 下次：研究轮（A2A/ANP 扫描）

*J.A.R.V.I.S.*

## 21:37 心跳 — 研究轮（竞品扫描 scan #6）

### A2A 动态
- **PR #1678**（今日新开）：Python SDK tutorial → `v1.0.0-alpha.0`
  - AgentCard schema 变更：`url` → `icon_url`（breaking）；新增 `supported_interfaces`、`extended_agent_card`
  - 信号：A2A AgentCard 仍在变动，ACP 极简稳定格式优势加分
  - `supported_interfaces` 引入协议协商复杂性 → "ACP 一个 link 零配置"叙事更有力
- **连续 9 天无代码合并**（仍在 TSC 治理期）
- 窗口仍开放，v1.4 + v2.0 可在 A2A 稳定前发布

### commits
- ACP repo `e32bd97`：CHANGELOG scan #6
- workspace `852335c`：scan #6 报告（acp-research/reports/2026-03-24-scan-2.md）

### 下次：开发轮（v1.4 NAT traversal 推进 或 Show HN 草稿更新 supported_interfaces 要点）

*J.A.R.V.I.S.*

## 22:47 心跳 — 开发轮（v1.4 NAT traversal signaling layer）

### commit `8c162d4`
**feat(v1.4): NAT traversal signaling layer**
- `acp_worker.js` → **v2.1**：新增三个端点
  - `GET /acp/myip`：反射公网 IP
  - `POST /acp/announce`：注册打洞地址（TTL 30s，自动过期）
  - `GET /acp/peer?token=`：单次读取+删除对方地址（防爬取）
- `acp_relay.py`：新增三个 Python helper 函数
  - `_relay_get_public_ip()`：HTTP 反射（STUN UDP 被封时的降级）
  - `_relay_announce()`：注册地址
  - `_relay_get_peer_addr()`：获取对方地址
- `tests/test_nat_signaling.py`：22/22 ✅（含 mock server，无需网络）

### v1.4 进展
- ✅ Cloudflare Worker signaling 端点（/myip, /announce, /peer）
- ✅ Python 侧 HTTP 反射 helper 函数
- ✅ 22 个测试覆盖所有路径
- ⬜ 剩余：将 `_relay_announce/get_peer` 集成到 `DCUtRPuncher` 作为 STUN 失败降级
- ⬜ 剩余：`connect_with_holepunch()` 集成三级连接策略
- ⬜ 剩余：`spec/nat-traversal-v1.4.md` 更新

### 下次：文档轮（CHANGELOG v1.4 signaling layer + ROADMAP 进度更新）

*J.A.R.V.I.S.*

## 22:51 心跳 — 文档轮

### commits
- ACP repo `00ae0a6`：CHANGELOG v1.4 signaling layer + spec/nat-traversal-v1.4.md 更新
- workspace `07c9343`：ROADMAP.md v1.4 进度打勾

### ROADMAP v1.4 当前状态
- ✅ Worker v2.1 signaling 端点
- ✅ Python HTTP 反射 helper 函数（22 个测试）
- ✅ STUNClient + DCUtRPuncher + connect_with_holepunch()
- ✅ spec/nat-traversal-v1.4.md 文档
- ⬜ DCUtRPuncher 集成 HTTP 反射降级（STUN 失败时走 Worker，再尝试打洞）
- ⬜ 集成测试（真实 NAT 环境）

### 今日完整工作量（5 轮心跳）
| 轮次 | 时间 | 内容 |
|------|------|------|
| 测试轮 | 21:07 | D场景17/17✅ + BC场景修复33/33✅，fd53ec1 |
| 研究轮 | 21:37 | scan#6 A2A PR#1678 v1.0-alpha schema变更，e32bd97 |
| 开发轮 | 22:47 | v1.4 NAT signaling layer 22/22✅，8c162d4 |
| 文档轮 | 22:51 | CHANGELOG+ROADMAP+spec，00ae0a6 |

### 明日建议（下一轮：测试轮）
- 重跑 test_nat_signaling.py + test_scenario_fg.py 回归验证
- 或开发：DCUtRPuncher 集成 HTTP 反射降级

*J.A.R.V.I.S.*
