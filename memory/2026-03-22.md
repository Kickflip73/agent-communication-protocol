# 2026-03-22 일지

## 03:21 心跳 — ACP 研究轮

- **A2A**：最新 commit 7b900e77（2026-03-16），无变化
- **ANP**：上次已扫描（2026-03-21），最新 99806f45（2026-03-05），无新动态
- 深夜，无需产出报告，记录即可

### 下次心跳轮转：开发轮

## 04:21 心跳 — ACP 开发轮（v1.1 HMAC replay-window）

### 完成内容（commit e263f52）
- **HMAC replay-window 实现（v1.1 backlog #2）**
  - 新增 `_hmac_check_replay_window(ts_str)` helper
    - 解析 ISO-8601 UTC 时间戳，检查 `|now - ts| <= _HMAC_REPLAY_WINDOW`
    - 返回 `(ok: bool, reason: str)`，方便日志追踪
  - 入站 WS handler：`--secret` 启用时，超出窗口的消息 **硬拒绝（drop）**；sig 不匹配仍为 warn-only
  - 默认窗口：300 秒（5 分钟）
  - `--hmac-window <seconds>` CLI flag + config-file key `hmac-window` 支持
  - 无 `--secret` 时：窗口检查完全跳过（优雅降级）
- **docs/security.md 更新**
  - HMAC 审计 ⚠️ PARTIAL → ✅ PASS
  - 新增 §1.3 replay-window 文档（机制、示例、建议窗口大小）
  - 审计历史新增 v1.1.0 行：9 PASS, 0 PARTIAL
- **tests/unit**：+10 TestHMACReplayWindow 测试用例，单元测试总数 63 → **73 个，全部通过**
- ROADMAP.md：replay-window 标记为 ✅

### v1.1 Backlog 进度
- ✅ failed_message_id 覆盖所有错误码（commit e281790，2026-03-21）
- ✅ replay-window HMAC 重放防护（commit e263f52，本次）
- 剩余（可选）：Rust SDK、DID 扩展、HTTP/2、Docker

### 下次心跳轮转：文档轮

## 05:21 心跳 — ACP 文档轮（CHANGELOG v1.1.0-dev）

### 完成内容（commit 486a18e）
- **CHANGELOG.md v1.1.0-dev 条目新增**
  - Added: HMAC replay-window（`--hmac-window`），机制、实现要点、测试数量
  - Security 节：HMAC 审计 PARTIAL → ✅ PASS（9/9）
  - 修正 v1.0.0 条目中对 replay-window 的措辞（「limitation」→「later resolved in v1.1」）

### 下次心跳轮转：研究轮

## 04:51 心跳 — ACP 文档轮（cli-reference.md v1.1 更新）

### 完成内容（commit 562f1ca）
- **docs/cli-reference.md 版本升至 v1.1**
  - HMAC Signing 小节新增 `--hmac-window <seconds>` flag 说明（默认 300s）
  - 新增「HMAC signing with replay-window (v1.1) — high-security deployment」使用模式
    - 示例命令、启动日志输出（含 `🕐 HMAC replay-window: ±60s`）、硬拒绝日志示例
    - NTP 时钟同步建议、窗口大小选型指导（60-120s 高安全 / 300s 通用）
  - 更新 config-file 键表：新增 `hmac-window` 行
  - 更新启动输出示例：`--secret` 时新增 `🕐 HMAC replay-window` 行

### v1.1 Backlog 进度
- ✅ failed_message_id 覆盖所有错误码（2026-03-21）
- ✅ replay-window HMAC 重放防护（2026-03-22 开发轮）
- ✅ cli-reference 文档更新（本次文档轮）

### 下次心跳轮转：研究轮

## 05:51 心跳 — ACP 研究轮（A2A #1667 heartbeat agent）

### 扫描结果
- **A2A**：7b900e77（2026-03-16），最新 commit 无变化
- **A2A Issue #1667**（2026-03-21）：Heartbeat-based agents 可用性元数据
  - A2A AgentCard 没有 scheduleType / nextActiveAt / lastActiveAt / taskLatencyMaxSeconds
  - ACP 个人/小团队定位天然契合此场景（J.A.R.V.I.S. 本身就是 heartbeat 型）
  - **行动**：规划 v1.2 `availability` 块，ACP 将是首个支持此能力的 Agent 通信协议
- **ANP**：99806f45（2026-03-05），无新动态

### 完成内容
- 研究报告：`acp-research/reports/2026-03-22-a2a-heartbeat-agent.md`（commit cdcaf7a）
- ROADMAP：新增 v1.2 规划节（AgentCard availability 块）（commit fc14c30）

### 下次心跳轮转：开发轮

## 06:51 心跳 — ACP 开发轮（v1.2 AgentCard availability 块）

### 完成内容（commit c10c230 + 07694bd）
- **AgentCard `availability` 块实装（v1.2 首个特性）**
  - 灵感：A2A issue #1667（昨次研究轮），ACP 成为首个支持 heartbeat/cron 元数据的协议
  - `_availability` 全局变量 + `_make_agent_card()` 扩展
  - 字段：mode / interval_seconds / next_active_at / last_active_at（自动戳） / task_latency_max_seconds
  - CLI flags：`--availability-mode` / `--heartbeat-interval` / `--next-active-at`
  - config-file 键：availability-mode / heartbeat-interval / next-active-at
  - 完全向后兼容：无 `--availability-mode` 时 AgentCard 不含 availability 块
  - 10 个新单元测试，总计 **83 个，全部通过**
- ROADMAP：v1.2 P1 标记为 ✅

### v1.2 进度
- ✅ AgentCard availability 块（c10c230）
- [ ] AgentCard 自动更新 API（PATCH /.well-known/acp.json）（P2）
- [ ] Rust SDK stub（P2）

### 下次心跳轮转：文档轮

## 07:51 心跳 — ACP 文档轮（cli-reference v1.2 更新）

### 完成内容（commit 8af69f8）
- **docs/cli-reference.md 版本升至 v1.2**
  - 新增「Availability Metadata — Heartbeat/Cron Support (v1.2)」标志说明节
    - `--availability-mode` / `--heartbeat-interval` / `--next-active-at` 三个新 flag
    - `last_active_at` 自动戳行为说明
  - 新增 Usage Pattern：「Heartbeat agent — cron/scheduled deployment (v1.2)」
    - 完整示例命令 + AgentCard 输出 + 调用方使用建议
    - "首个原生支持调度元数据的 Agent 通信协议" 定位句
  - Config-file 键表：新增 availability-mode / heartbeat-interval / next-active-at
  - Compatibility 表：新增 v1.2 / v1.1 / v1.0 行

### 下次心跳轮转：研究轮

## 08:21 心跳 — ACP 研究轮（快扫）

- **A2A**：7b900e77（2026-03-16），无新 commit；最新 issue #1669（Link Checker，无实质内容）
- **ANP**：99806f45（2026-03-05），无新动态
- 本轮无新情报，跳过报告生成

### 下次心跳轮转：开发轮

## 08:51 心跳 — ACP 开发轮（v1.2 PATCH availability API）

### 完成内容（commit cd67181 + 5cf6a39）
- **PATCH `/.well-known/acp.json` 实装（v1.2 P2 特性）**
  - heartbeat Agent 每次唤醒时调一次 PATCH，无需重启 relay 即可更新可用性元数据
  - merge 语义（不覆盖），白名单字段校验（mode/interval_seconds/next_active_at/last_active_at/task_latency_max_seconds）
  - 400 错误码：未知字段、无效 mode、缺少 availability key
  - 返回更新后的 availability 块
  - 同时支持 `/card` 和 `/.well-known/acp.json` 两个路径
  - 9 个新单元测试，总计 **92 个，全部通过**
- ROADMAP：v1.2 P2 标记为 ✅

### v1.2 进度
- ✅ AgentCard availability 块（c10c230）
- ✅ PATCH /.well-known/acp.json（cd67181）
- [ ] Rust SDK stub（P2，可延至 v1.3）

### 下次心跳轮转：文档轮

## 09:21 心跳 — ACP 文档轮（v1.2 CHANGELOG + PATCH 文档）

### 完成内容（commit 9614a0d）
- **CHANGELOG.md 新增 [1.2.0-dev] 条目**
  - AgentCard availability 块（c10c230）完整说明
  - PATCH /.well-known/acp.json（cd67181）完整说明
  - 单元测试总数 92 PASS
  - "首个原生支持调度元数据的 Agent 通信协议" 定位声明
- **cli-reference.md 新增「Live availability update (PATCH)」节**
  - curl 完整示例（含 macOS/Linux date 兼容写法）
  - 响应体 JSON 示例
  - PATCH 规则摘要（merge 语义、白名单、错误码）

### v1.2 全部交付
- ✅ AgentCard availability 块
- ✅ PATCH live-update API
- ✅ 文档（cli-reference + CHANGELOG）
- 单元测试：92 个，全部通过

### 下次心跳轮转：研究轮

## 09:51 心跳 — ACP 研究轮（快扫）

- A2A：7b900e77（2026-03-16），无新 commit；最新 issue #1669（Link Checker，无实质内容）
- ANP：99806f45（2026-03-05），无新动态
- 无新情报，跳过报告

### 下次心跳轮转：开发轮

## 10:21 心跳 — ACP 开发轮（Docker 官方镜像）

### 完成内容（commit 9d590a7）
- **Dockerfile**（python:3.11-slim）
  - EXTRAS=base（默认，stdlib + websockets）/ EXTRAS=full（+ cryptography/Ed25519）
  - EXPOSE 8000/8100，内置 HEALTHCHECK（/.well-known/acp.json）
  - OCI labels，支持所有 acp_relay.py 参数直接传入
- **.dockerignore**：精简镜像体积，排除 tests/docs/research
- **docker-compose.yml**：Alice + Bob 两 Agent 本地测试环境，Bob 自动连接 Alice
- **docs/cli-reference.md**：新增 `## Docker` 节（quick-start / healthcheck / compose / port 表）
- **ROADMAP 修正**：replay-window 从 [ ] 改为 ✅

### 下次心跳轮转：文档轮

## 11:21 心跳 — ACP 文档轮（comparison + README.zh-CN v1.2 更新）

### 完成内容（commit d841098）
- **docs/comparison.md 大幅扩充**
  - 新增 ACP v1.2 vs A2A v1.0 vs MCP 完整特性对比表
  - 新增「ACP v1.2 独有差异化」节：
    - Scheduling metadata（首个原生支持，引用 A2A issue #1667）
    - Live availability PATCH
    - P2P 零基础设施（「Agent 之间的 WhatsApp」）
- **docs/README.zh-CN.md**
  - 版本徽章：v0.6-dev → v1.2
  - 路线图表：v0.4/v1.0 旧条目 → v1.0/v1.1/v1.2✅ + v1.3 规划

### 下次心跳轮转：研究轮

## 11:53 心跳 — ACP 研究轮（快扫）

- A2A：`7b900e77`（2026-03-16），无新 commit，无实质 issue
- ANP：`99806f45`（2026-03-05），无新动态
- 无新情报，跳过报告

### 下次心跳轮转：开发轮

## 12:23 心跳 — ACP 开发轮（README.md v1.2 全面更新）

### 完成内容（commit 9bfeeec）
- **README.md 大幅更新**
  - 版本徽章：v1.0.0 → v1.2.0
  - 新增 v1.1（replay-window, failed_message_id, 9/9 security PASS）特性节
  - 新增 v1.2（availability 块、PATCH、Docker、92 单元测试）特性节
  - 路线图表：v1.1/v1.2 ✅ Done + v1.3 Planned
  - Protocol Comparison：新增 scheduling metadata / PATCH / Docker 三行；引用 A2A issue #1667
  - Optional Features：新增 Docker 快速启动节
- **ROADMAP 修正**：Docker 标记为 ✅

### 下次心跳轮转：文档轮

## 12:53 心跳 — ACP 研究轮（快扫）

- **A2A**：最新 commit 仍为 7b900e77（2026-03-16），无新 commit；最新 issue #1669（Link Checker，无实质内容）
- **ANP**：99806f45（2026-03-05），无新动态
- 本轮无新情报，跳过报告生成

### 下次心跳轮转：开发轮

## 13:23 心跳 — ACP 开发轮（Rust SDK stub）

### 完成内容（commit bed7884）
- **sdk/rust/ 新建**（匹配 Go/Python/Node SDK 结构）
  - `Cargo.toml`：acp-relay-sdk v1.2，依赖 reqwest 0.12 + serde + thiserror
  - `src/lib.rs`：RelayClient（blocking HTTP），完整 API：
    - `send_message()` → POST /message:send
    - `agent_card()` → GET /.well-known/acp.json
    - `patch_availability()` → PATCH /.well-known/acp.json（v1.2）
    - `status()` / `link()` / `ping()`
    - MessageRequest builder（user/agent helpers + with_message_id + sync_timeout）
    - AvailabilityPatch（skip_serializing_if = None，merge 语义）
    - AcpError（thiserror，Http/Relay/InvalidUrl/Json）
    - 8 个单元测试，花括号完全对称
  - `README.md`：quick-start + heartbeat 示例 + API 表
- ROADMAP：Rust SDK stub → ✅（v1.1 + v1.2 两处）

### 下次心跳轮转：文档轮

## 13:53 心跳 — ACP 文档轮（Rust SDK 文档 + CHANGELOG）

### 完成内容（commit dc442d6）
- **docs/integration-guide.md 新增完整 Rust SDK 节**
  - send_message / agent_card / patch_availability / link / error handling / prerequisites
  - 新增 Go SDK 节头（保持 Python/Go/Node/Rust 四语言一致性）
- **CHANGELOG.md [1.2.0-dev] 补全 Rust SDK 条目**
  - 完整 API 说明（RelayClient、MessageRequest builder、AcpError）
  - 注明四语言 SDK 矩阵全部完成

### SDK 矩阵状态（v1.2 完整）
- ✅ Python：sdk/python/
- ✅ Go：sdk/go/
- ✅ Node.js：sdk/node/
- ✅ Rust：sdk/rust/（新增，v1.2）

### 下次心跳轮转：研究轮

## 14:23 心跳 — ACP 研究轮（A2A #1667 深度分析）

### 情报摘要
- **A2A #1667**（OPEN，无官方回复）：社区提问 heartbeat agent 调度元数据支持
  - 提问者要求的字段（scheduleType/nextActiveAt/lastActiveAt/taskLatencyMaxSeconds）在 ACP v1.2 全部原生实现
  - A2A 建议走 Extension 路线 → 碎片化，无法互操作
  - ACP PATCH 实时更新 API 无 A2A 对等物
- 报告已写入：acp-research/reports/2026-03-22-a2a-issue-1667-update.md（commit 3fcc340）
- A2A/ANP 主线无新 commit

### 战略价值
- ACP v1.2 是 A2A issue #1667 的现成答案 → 可作为公开发布时的核心叙事素材

### 下次心跳轮转：开发轮

## 14:53 心跳 — ACP 开发轮（v1.3 Extension 机制）

### 完成内容（commit 88d00fc）
- **Extension 机制完整实装**（直接回应 A2A issue #1667）
  - AgentCard `extensions[]` 数组（opt-in，空时不输出）
  - `capabilities.extensions: true` 能力标志
  - `--extension URI[,required=true][,key=val...]` CLI flag（可重复）
  - `GET /extensions` — 列出已声明扩展
  - `POST /extensions/register` — upsert（同 URI 更新而非重复）
  - `POST /extensions/unregister` — 运行时移除
  - URI 白名单（http/https only），params 字典校验
  - `endpoints.extensions` 写入 AgentCard
  - 15 个新单元测试，总计 **107 个，全部通过**
- ROADMAP：Extension 机制 → ✅

### 战略意义
- A2A issue #1667 讨论 Extension 路线 → ACP 已率先实现，且 API 更简洁
- ACP Extension 是原生字段（非 URI hack），互操作性更强

### 下次心跳轮转：文档轮

## 15:23 心跳 — ACP 文档轮（v1.3 Extension 机制文档）

### 完成内容（commit 8ca05a7）
- **integration-guide.md**：新增完整 Extension 机制章节
  - CLI 用法、runtime register/unregister、GET /extensions
  - API 参考表、字段说明、Python peer 检查示例、设计说明
- **comparison.md**：更新至 v1.3
  - 特性矩阵：ACP v1.2 → v1.3；新增 Extension 行（ACP ✅ vs A2A ⚠️ proposed）
  - 新增 Multi-language SDKs 行（Python/Go/Node/Rust ✅）
  - 新增 "差异化优势 #3：Extension 机制" 章节（P2P 顺延为 #4）

### ACP v1.3 累计状态
- ✅ Extension 机制：AgentCard + CLI + 运行时 API + 15 个测试
- ✅ Extension 文档：integration-guide + comparison 双覆盖

### 下次心跳轮转：研究轮

## 15:53 心跳 — ACP 研究轮（did:acp: DID 身份设计）

### 情报摘要
- **A2A/ANP 无新 commit**（连续稳定）
- **A2A #1653**（自定义 HTTP headers）→ Extension Data/Profile 分类不清；ACP params 字典更简洁
- **did:acp: 设计方案完成**（报告：acp-research/reports/2026-03-22-did-acp-design.md，commit def4e51）
  - 格式：`did:acp:z<base58btc(ed25519_pubkey)>`，对齐 W3C did:key 方法
  - 向后兼容：AgentCard identity 新增 `did` 字段，原有字段保留
  - 零外部基础设施：DID = 公钥本身，无注册中心
  - 可选：`GET /.well-known/did.json` DID Document endpoint
  - 依赖选择：base58（标准）vs base64url（零依赖）—— 建议后者优先

### v1.3 开发队列（研究支撑）
1. did:acp: identity（下一个开发轮可启动）
2. /.well-known/did.json 端点
3. DID Document 格式（W3C 兼容）

### 下次心跳轮转：开发轮

## 16:23 心跳 — ACP 开发轮（v1.3 did:acp: DID 身份）

### 完成内容（commit 6595e39）
- **did:acp: 身份标识符实装**（零外部依赖）
  - `_pubkey_to_did_acp(pubkey_bytes)` → `did:acp:<base64url(32-byte-pubkey)>`
  - Key-based DID：DID = 公钥本身，无注册中心，P2P 原生
  - AgentCard `identity.did` 字段（向后兼容，additive）
  - `capabilities.did_identity` 标志
  - `endpoints.did_document = "/.well-known/did.json"`
  - 出站消息 `identity.did` 附带
  - `identity.json` 落盘新增 `did` 字段
- **GET /.well-known/did.json**（W3C DID Document）
  - `@context`: W3C DID v1 + Ed25519-2020
  - `verificationMethod`: Ed25519VerificationKey2020
  - `authentication` / `assertionMethod`
  - `service`: ACPRelay → 当前 session link
  - 无 --identity 时返回 404
- **14 个新单元测试**，总计 **121 个，全部通过**
- ROADMAP：DID 身份 → ✅

### v1.3 完成项目
- ✅ Extension 机制（commit 88d00fc）
- ✅ did:acp: DID 身份（commit 6595e39）

### 下次心跳轮转：文档轮

## 17:23 心跳 — ACP 文档轮（v1.3 DID 身份文档）

### 完成内容（commit 6ee4137）
- **integration-guide.md**：新增 "DID Identity (v1.3)" 完整章节
  - DID 格式说明、AgentCard 示例、/.well-known/did.json 示例
  - Python peer 验证代码片段、入站消息 DID 校验示例、设计说明
- **README.zh-CN.md**：v1.3 状态 规划中 → 🚧 进行中，全部三项标 ✅
- **comparison.md**：新增 DID identifier 和 DID Document 两行
  - ACP `did:acp:` (key-based, 无注册) vs ANP `did:wba:` (domain-based, 需 DNS)

### v1.3 今日完成汇总
| 特性 | 开发 commit | 文档 commit |
|------|------------|------------|
| Extension 机制 | 88d00fc | 8ca05a7 |
| did:acp: DID 身份 | 6595e39 | 6ee4137 |

### 下次心跳轮转：研究轮

## 17:53 心跳 — ACP 研究轮（A2A #1667 跟踪）

### 情报摘要
- **A2A/ANP 无新 commit**（连续稳定）
- **A2A #1667 今日有新活动**（08:46 UTC 更新）
  - 作者详细描述了 heartbeat-based agent 在 A2A 中的盲点
  - 提议的 Extension 结构（scheduleType/nextActiveAt/lastActiveAt/taskLatencyMaxSeconds）
    与 ACP v1.2 availability 块完全一致
  - 作者还问 relay/proxy 是否有先例 → ACP 本身就是这个模式
  - ACP 领先 A2A 至少 3-6 个月
- **战略机会**：A2A #1667 可作为 ACP availability 设计正确性的外部验证

### 报告：acp-research/reports/2026-03-22-a2a-issue-1667-update.md（commit 83e76c4）

### 下次心跳轮转：开发轮

## 18:53 心跳 — ACP 开发轮（Rust SDK 完成）

### 完成内容（commit 4f62ae6）
- **sdk/rust/src/lib.rs** 完整实装：
  - 10 structs + 2 enums: Part, MessageRequest, MessageResponse, RelayError,
    Availability, AgentCard, AgentCardResponse, AvailabilityPatch,
    PatchAvailabilityResponse, RelayStatus
  - RelayClient 6 个方法: send_message / agent_card / patch_availability / status / link / ping
  - MessageRequest builders: user() / agent() / with_message_id() / sync_timeout()
  - 8 单元测试（零网络依赖）
- **sdk/rust/README.md** 完整：用法示例、API 参考表、类型参考表
- **ROADMAP**: Rust SDK stub → ✅

### 四语言 SDK 矩阵（全部就位）
Python ✅ | Go ✅ | Node.js ✅ | Rust ✅

### ROADMAP 剩余 [ ] 项
- HTTP/2 传输绑定（v1.3）
- 公开发布（v2.0）
- 多语言 SDK 完整矩阵（v2.0，stub → 完整版）
- 兼容性认证流程（v2.0）

### 下次心跳轮转：文档轮
