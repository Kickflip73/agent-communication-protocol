# 2026-03-28 — ACP 协议研发日志

## 心跳周期汇总（全天）

### 上午轮（约 12:17）
- **测试轮**：场景 C（流水线 A→B→C→A）✅、场景 D（压力 100条）✅
- **BUG-036**：`/peer/{id}/send` 响应缺 `server_seq` 字段 → 已修复，commit `da09a6f`
- **Node SDK v2.1.0**：Extension 类 + hasExtension/requiredExtensions，32/32 PASS，commit `8245c08`
- **场景 A/B 测试**：127.0.0.1 问题确认并修复，WS 连接 0.5s 成功

### 上午轮（约 12:51）
- **研究轮**：A2A/ANP 周报扫描 #16，A2A `limitations` 提案（Issue #1694，2026-03-27）发现 → 触发 v2.7 开发

### 下午轮（约 13:51）
- **开发轮**：v2.7 AgentCard `limitations` 字段实现，20/20 PASS
- **BUG-037**：`messages_received` 多 peer 场景始终为 0 → 已修复，commit `ced26b3` / `cb1fbfa`
  - 根因：BUG-005 修复不完整，`_from` 绑定时序问题（agent_name 未及时写入）
  - 修复：懒绑定逻辑，`_on_message` 中匹配最新未命名 peer

### 下午轮（约 14:36）
- **测试轮（本心跳必须）**：
  - 场景 E（NAT 三级降级）：14/14 PASS（1 端口竞态抖动，重跑通过）
  - 场景 B+D：16/16 PASS（1 env-gate skip）
  - 全套回归（6 套件）：90/90 PASS
  - **总计：121 passed, 2 skipped, 0 failed, 0 new bugs**
- **文档轮**：
  - CHANGELOG 补全 v2.8/v2.9/v3.0 条目（Extension + LangChain + GET /messages + NAT集成）
  - Show HN 草稿更新至 v3.0，shipping list 全面刷新
  - commit `54f7d70`

### 下午轮（约 14:44）
- **开发轮**：v2.10.0 Skills-lite
  - `GET /skills` 端点：tag/q/limit/offset 过滤 + 分页
  - AgentCard `skills` 字段升级为结构化对象数组
  - `--skills` CLI 向后兼容（CSV 字符串自动转换）
  - `tests/test_skills_list.py`：SK1–SK6，**6/6 PASS**
  - CHANGELOG v2.10.0 条目添加
  - commit `f303c5e`（push 成功）

## 版本演进（今日）

| 版本 | 主题 | commit |
|------|------|--------|
| v2.7.0 | AgentCard `limitations` 三元能力边界 | `49935ac` |
| v2.8.0 | Extension 机制 + LangChain adapter + Node SDK v2.1.0 | `2eaa357`+`7584e6e`+`8245c08` |
| v2.9.0 | `GET /messages` 历史消息列表 | 今日研发中 |
| v3.0.0 | `_connect_with_nat_traversal()` NAT三级自动降级 | 本轮 |
| v2.10.0 | Skills-lite `GET /skills` + 结构化 AgentCard skills | `f303c5e` |

## Bug 统计（今日）

| Bug | 优先级 | 状态 | commit |
|-----|--------|------|--------|
| BUG-036 | P2 | ✅ 已修复 | `da09a6f` |
| BUG-037 | P2 | ✅ 已修复 | `ced26b3` |

## 下次心跳

- **next_round = docs**
- 任务：ROADMAP 更新（v2.3/v2.8/v2.9/v2.10 里程碑补全）+ whats-new.md v2.10 节 + README vs-A2A 新行
- 测试（必须）：场景 G（断线重连）+ 场景 H（并发）
