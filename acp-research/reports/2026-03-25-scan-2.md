# ACP 竞品研究扫描 #11 — 2026-03-25 16:28

## A2A 最新动态

### Commit 状态
- **最新 commit**: 2026-03-16（`7b900e7` CODEOWNERS TSC 治理）
- **代码冻结持续 9 天**，无新功能合并
- 仍处于 TSC 稳定治理模式

### 今日新 Issues（2026-03-25）

#### #1684 [Bug] CancelTaskRequest 定义缺失 + cancel 语义仍未厘清
> 三个问题：
> 1. `CancelTaskRequest` 类型定义缺失
> 2. 取消后 SSE 长连接应该怎么处理（返回 CANCELED 状态 还是 直接断开？）
> 3. 若 task 无法立即取消，cancel API 返回什么状态？

**ACP 对比优势：**
- ACP `spec §10`（commit `0d19a11`）已明确定义三种 cancel 场景
- cancel 是**同步即时**的：`:cancel` 返回最终 `canceled` 状态，**同一 HTTP response**
- SSE 长连接处理：已在 spec 中说明（cancel 后推送 `status` 事件 + 关闭流）
- A2A 连 `CancelTaskRequest` 的定义都还在讨论，ACP 已有完整 spec 两周了

#### #1683 [Bug] SSE 示例中 `contextId` 字段缺失（与 §4.2.2 规定矛盾）
> `TaskArtifactUpdateEvent` 中 `contextId` 是 mandatory，但 §6.2 流式执行示例中没有返回。

**ACP 对比：**
- ACP `context_id` 自 v0.7 起支持，测试覆盖完整，无此文档/实现不一致问题

#### #1672 Proposal: Agent Identity Verification for Agent Cards（2026-03-24 更新）
> 提案：给 AgentCard 加身份验证机制

**ACP 对比：**
- ACP v1.3 已实现 `did:acp:` DID 身份（commit `6595e39`）
- Ed25519 可选身份扩展（v0.8，commit `1a13dec`）
- **ACP 比 A2A 提案早实现了 DID 方向，且已在 production code 中**

---

## 竞争情报摘要

| 维度 | A2A 当前状态 | ACP 状态 |
|------|-------------|---------|
| Cancel 语义 | #1680 + #1684 仍在讨论，CancelTaskRequest 定义缺失 | ✅ spec §10 完整定义，同步取消 |
| SSE 文档质量 | #1683 contextId 矛盾 | ✅ 无此不一致 |
| 身份验证 | #1672 提案阶段 | ✅ DID + Ed25519 已实现 |
| 代码活跃度 | 冻结 9 天 | ✅ 今日 163 测试全绿 |

---

## Show HN 更新建议

基于今天的 A2A #1684，Show HN 草稿可强化 cancel 语义这一段：

> "A2A 用了一年还没说清楚 cancel 该怎么做（#1680, #1684），
> ACP 的答案很简单：cancel 是同步的，调用即返回最终状态，一个 HTTP round-trip 搞定。"

---

## 行动项

- [ ] 更新 `docs/show-hn-draft.md`，加入 #1684 引用强化 cancel 差异化
- [ ] v1.4 下一步：`_connect_with_nat_traversal()` 集成（开发轮推进）
- [ ] 考虑发 Show HN：A2A 9+ 天无大更新，时机持续成熟
