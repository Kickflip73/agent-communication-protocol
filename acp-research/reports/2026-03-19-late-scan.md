# 竞品情报扫描 — 2026-03-19 深夜

## 扫描时间
2026-03-19 22:39 (Asia/Shanghai)

## A2A 新动态（2026-03-16 后无新 commits）

### 值得关注的新 Issues（2026-03-18 更新）

#### #1619 — 自定义协议绑定文档
- 新增 `custom-protocol-bindings.md`：区分「Extension」（消息字段扩展）和「Protocol Binding」（整个传输层替换）
- ACP 参考：我们的 P2P (`acp://`) vs Relay (`acp+wss://`) 正是「双绑定」架构，可借鉴此文档结构完善 `spec/transports.md`

#### #1655 — QuerySkill() 提案
- A2A 正在讨论运行时能力查询 API，与我们 v0.5 中已实现的 `POST /skills/query` 高度一致
- ACP 版本更轻量（无 schema 验证开销），定位正确

#### #1658 — AgentSkill SecurityRequirement 断链 bug  
- 说明 A2A 规范仍在高速演进；ACP 坚持「连接层认证，不在任务层处理」更稳定

## ANP 新动态（2026-03-05）

### `failed_msg_id` 字段（commit 99806f45）
- E2EE 错误消息加入 `failed_msg_id`：接收方可精确告知发送方哪条消息失败
- **ACP 借鉴**：v0.6 错误码规范中加入 `failed_message_id` 字段，支持精确重传

### `handle` 特性（commit 761087d5）
- commit 信息简略，推测为消息处理句柄/回调注册，待观察

## 关键洞察

1. **传输绑定分层**：A2A #1619 的 Extension vs Binding 分层值得 ACP 借鉴，重组 transports.md
2. **错误追踪**：ANP `failed_msg_id` 是实用设计，ACP v0.6 错误码规范纳入
3. **QuerySkill 对齐**：ACP 实现先于 A2A，且更轻量 — 差异化优势确立

## v0.6 行动项（加入 ROADMAP）
- [ ] 重组 spec/transports.md（参考 A2A Protocol Binding 分层）
- [ ] 错误响应加入 `failed_message_id` 字段（参考 ANP）
