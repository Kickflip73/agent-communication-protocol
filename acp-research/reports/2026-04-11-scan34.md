# ACP Research Scan #34 — 2026-04-11

> **扫描时间**：2026-04-11 10:54 CST  
> **上一轮**：scan33（2026-04-11，ANP DataIntegrityProof + origin_proof，A2A #1716 Capability Token）  
> **ACP 当前版本**：v3.2.0

---

## A2A 最新动态

### 仓库状态
- **仓库已迁移**：`google-a2a/A2A` → `a2aproject/A2A`（重定向有效）
- **近期活跃**：Apr 9–10, 2026 有多次 commit；社区 issue 讨论热度持续上升

### 重点 Issue 追踪

#### #1716 — RFC: Capability Token 在 AgentSkill 边界的授权执行（持续跟踪）
- **提案方**：SINT Protocol（Apache 2.0，`sint-ai/sint-protocol`）
- **核心机制**：Ed25519 capability token，编码 subject、resource、actions、tier（T0-T3）、constraints、exp
- **Tier 分级**：
  - T0/T1（observe/read）→ 自动执行
  - T2（act，如 send_notification）→ 运营商审批
  - T3（commit，如 transfer_funds/deploy_code）→ 人工签名授权
- **与 A2A 关系**：A2A 处理「谁在调用」，SINT 处理「允许做什么」，互补不替代
- **ACP 相关性**：⭐⭐⭐⭐⭐ 高度相关——ACP v3.3 可考虑对接 capability token 机制

#### #1717 — Governance metadata in A2A Agent Cards（微软提案）
- **提案方**：Microsoft `agent-governance-toolkit`
- **内容**：AgentCard 携带 trust score、capability manifest、policy compliance（ATF/OWASP）、audit trail reference
- **意图**：接收方在接受任务前能做信任决策
- **ACP 相关性**：⭐⭐⭐ 中等——可为 ACP agent_card 扩展字段提供参考

#### #1718 — Bilateral signed interaction records as trust primitive
- **核心观点**：将 identity、trust scoring、audit、delegation 统一为「双边签名交互记录」单一数据结构
- **问题诊断**：当前各提案（#1575、#1628、#1501、#1583、#1716）彼此孤立，无法组合
- **ACP 相关性**：⭐⭐⭐⭐ 提供了重要设计参考——ACP 的 DataIntegrityProof 签名链可对接此方向

#### #1713 — OBO（On Behalf Of）跨组织授权标准（早期 RFC）
- **场景**：跨组织、无共享 AS、首次接触的 A2A 事务
- **构件**：OBO Credential（pre-task，DNS 锚定）+ OBO Evidence Envelope（post-task，防篡改）
- **Go 参考实现**已运行
- **ACP 相关性**：⭐⭐⭐ 与 ACP 的 origin_proof 设计方向吻合

#### #1721 — Task lifecycle evidence（外部消费者视角）
- **内容**：最小化 A2A task 生命周期证据 seam，task.requested + task.updated → Assay-shaped evidence
- **ACP 相关性**：⭐⭐ 可参考证据格式

#### #1739 — Strata：AI Agent 考古游戏
- **性质**：社区展示项目，非规范讨论，ACP 无需关注

### 近期 Commits 节奏
- Apr 7–9：多次 commit，具体标题未能完整提取（GitHub readability 遮掩）
- Mar 12：密集 commit（13+ 次），推测为某个大版本合并
- 整体节奏：仍活跃，非停滞状态

---

## ANP 最新动态

### 仓库活跃度
- **极度活跃**：Apr 3–10, 2026 共 20+ commits（约 8 天内）
- Apr 10 单日 3 commits，Apr 9 单日 8 commits（最高活跃）
- 提交频率高于上轮 scan33 观测

### 架构状态（来自 README 最新版）
ANP 三层架构维持稳定：
1. **Identity and Secure Communication Layer**：基于 W3C DID，去中心化身份认证 + 端到端加密
2. **Meta-Protocol Layer**：代理间协议协商（自组织、自协商）
3. **Application Protocol Layer**：基于 Semantic Web 规范，能力描述与应用协议管理

### DID-WBA 文档状态
- `docs/did-wba-server-test-interface.md` 存在 → 测试接口文档已更新
- `docs/did-wba入门指南.md` 存在 → 中文入门指南维护中
- `docs/how-anp-based-webagent-works.md` 存在

### DataIntegrityProof / multikey / ECDSA-SD 动态
- 本轮无法直接提取近期 commit 标题（GitHub readability 限制）
- 基于 README 确认：ANP 仍以 W3C DID + 密码学证明为核心
- **待验证**：ECDSA-SD 选择性披露是否已进入 ANP 实现层（下轮需用 GitHub API 提取 commit 消息）

---

## W3C DataIntegrityProof 规范状态

### 当前版本
- **Verifiable Credential Data Integrity**：正在编辑的版本为 **v1.1**（Working Draft，来自 w3c.github.io）
- `/TR/vc-data-integrity/` 稳定发布页被 Cloudflare 拦截（403），以 Editor's Draft 信息为准

### 核心机制（v1.1）
三步模型维持稳定：
1. **Transformation**（规范化，如 canonicalization / binary-to-text）
2. **Hashing**（SHA-3、BLAKE-3 等）
3. **Proof Generation / Verification**（数字签名、proof of knowledge 等）

### 加密套件状态
- **Ed25519Signature2020**（EdDSA）：已稳定，被 ACP v3.2.0 采用 ✅
- **ecdsa-rdfc-2019**（P-256/P-384）：已发布独立套件规范 `vc-di-ecdsa`（TR 页面暂时不可访问）
- **multikey 支持**：属于 DID 密钥格式规范，与 Data Integrity 兼容，但需显式声明 `verificationMethod.type = Multikey`
- **ECDSA-SD（选择性披露）**：`vc-di-ecdsa` 套件中的子特性，支持 JSON Pointer 指定的部分声明披露，规范仍在 Candidate Recommendation 阶段（**非 W3C Recommendation**）

### 可行性评估
| 特性 | 状态 | ACP 可用性 |
|------|------|-----------|
| Ed25519Signature2020 | W3C REC | 已集成 ✅ |
| ecdsa-rdfc-2019 | CR | 可引入，需 P-256 支持 |
| multikey key format | DID Spec REC | 可与 Ed25519/P-256 联用 ✅ |
| ECDSA-SD selective disclosure | CR (不稳定) | 待观察，暂不建议 v3.3 主线集成 |

---

## ACP 影响评估（v3.3 开发目标建议）

### 核心发现摘要

1. **A2A 授权层收敛趋势明显**：#1716（SINT Capability Token）、#1717（Governance metadata）、#1718（双边签名记录）三个提案指向同一方向——需要标准化的「技能级授权」机制。ACP 若提前设计兼容 capability token 的 `authorization_proof` 字段，可在 A2A 标准化前占据先发优势。

2. **ANP 活跃度远超 A2A**：ANP 近 8 天 20+ commits，而 A2A 活跃在社区 issue 讨论层面，spec 本身修改较少。ANP 在 DID 层的演进速度更快，ACP 应紧密跟踪。

3. **W3C ECDSA-SD 尚未 REC**：选择性披露仍是 Candidate Recommendation，不应作为 v3.3 必选特性，但可设计预留扩展点。

4. **跨组织信任空白是最大机会点**：#1713（OBO）和 #1718（双边签名）均在填补「无共享 AS 的跨组织首次接触」场景。ACP 的 `origin_proof` 已部分覆盖，v3.3 可强化为完整的「跨域首接触授权协议」。

### v3.3 目标建议
1. **实现 capability token 兼容层**：在 ACP task metadata 中预留 `capability_token` 字段，参考 SINT T0-T3 tier 模型
2. **强化 origin_proof 为跨域授权凭证**：参考 OBO Credential 格式，使 ACP 的 origin_proof 可携带 principal_id、operator_id、governance_framework_ref
3. **multikey 预留支持**：verificationMethod 增加 `type: Multikey` 选项，为未来多算法套件切换铺路
4. **ECDSA-SD 扩展点**：在 proof 结构中预留 `selectiveDisclosure` 字段，不实现但保留 schema 空间

---

## 行动建议（1-3条）

1. **【立即】设计 `capability_token` 扩展字段规范**  
   参照 A2A #1716 SINT 协议的 T0-T3 tier 模型，在 ACP v3.3 spec 中定义 `task.metadata.capability_token` 结构。这是当前 A2A 社区最热的授权讨论点，提前兼容有利于 ACP 生态对接。

2. **【近期】ANP commit 精细扫描（下轮 scan35 核心任务）**  
   本轮无法提取 ANP commit 标题，建议 scan35 使用 GitHub API（`api.github.com/repos/agent-network-protocol/AgentNetworkProtocol/commits`）直接获取 JSON 格式的 commit 列表，重点定位 multikey 和 ECDSA-SD 相关改动。

3. **【观察】W3C vc-di-ecdsa CR 进程**  
   ECDSA-SD 选择性披露仍在 Candidate Recommendation 阶段。建议在 W3C 官方发布 Proposed Recommendation 通知前，ACP 不将其纳入 v3.3 主线实现，但在 spec 文档中标注为「计划支持」。

---

*本报告由 J.A.R.V.I.S. ACP 研究子代理自动生成，基于 2026-04-11 实时抓取数据。*  
*下轮研究（scan35）建议类型：开发轮（capability_token 字段实现）。*
