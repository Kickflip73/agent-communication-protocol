# ACP Research Scan #22 — 2026-04-07

**Scan type**: 🟢 ONLINE (web_fetch 可达，Brave API key 未配置)
**Baseline**: scan18 (2026-04-07 在线) → scan20/scan21 (2026-04-07 离线推断)
**Confidence**: ~85% (在线主源可达，评论截断，无全量新评论)
**ACP current**: v2.77.0

---

## #1716 — SINT capability token enforcement (ACP v2.74/v2.75/v2.77 核心参考)

**状态**: Issue 正常开放（title 确认不变）
- 标题：「RFC: Authorization layer for A2A AgentSkill invocations — capability token enforcement at skill boundaries」
- @pshkv SINT Protocol 提案内容已确认（Ed25519 capability token，T0-T3 tier，subject/resource/actions/constraints/exp）
- ACP SINT 三元组与之完全对齐：
  - v2.74: `GET /trust/signals/capability-token` (declaration)
  - v2.75: `GET /trust/signals/capability-token/fixtures` (4-deny+1-allow vectors)
  - v2.77: `POST /trust/signals/capability-token/fixtures/validate` (runtime enforcement)

**评论全量状态**: 无法确认（web_fetch 截断），上次在线扫描(scan18)为 7💬
**@pshkv spec PR 状态**: 待确认（截断后无法看到最新评论）；推断仍在 draft 中

**ACP v2.77 对齐验证**:
- `subject` binding check ✅（token.sub == invoking_agent_did）
- `resource` scope check ✅（resource 尾部 == target_skill_id）
- `exp` TOCTOU re-check ✅（use_time not check_time）
- `required_fields` presence ✅（jti/iss/sub/resource/scheme）
- `deny_details` audit trail ✅（A2A #1716 enforcement evidence）

---

## #1717 — Governance metadata in AgentCard (ACP 对齐参考)

**状态**: 已确认存在且标题不变
- 标题：「Proposal: Governance metadata in A2A Agent Cards」
- 来源：microsoft/agent-governance-toolkit，@imran-siddique
- 核心提案：AgentCard 携带 trust_score / capability_manifest / policy_compliance / audit_trail_reference
- ACP 现状：v2.64 已有 governance_attestation trust signal（领先），v2.71 security_posture（独立端点）

**v2.78 候选 B 评估（governance_metadata v2）**:
- `policy_compliance` — ACP 可在 security_posture 里扩展（severity/category 已有分层）
- `audit_trail_reference` — ACP 已有 bilateral_ir_log + interaction_records，可加 permalink 字段
- `capability_manifest` — ACP skills 端点已有，可加 machine-readable 格式字段
- **结论**: v2.78 候选 B 可行，但 ACP 各层已覆盖大部分，增量价值为「显式 AgentCard 字段」

---

## #1628 — trust.signals[] (ACP v2.68-v2.71 核心参考)

**状态**: 已确认（title 不变）
- 标题：「trust.signals[] extension: consolidated signal type specification」
- 5-signal 分类：onchain_credentials/onchain_activity/vouch_chain/behavioral/governance_attestation
- 共同验证模式：ECDSA/EdDSA 签名
- ACP 当前：13 种 trust signal（覆盖并扩展了原始 5 种）

**ACP 差异化**: ACP 的 `security_posture` + `bilateral_ir` + `watermark` 超越原始 5 种分类；`severity`/`category` 元数据给消费方提供加权基础（@kenneives v2.70 方向验证正确）

---

## #1718 — Bilateral signed interaction records (ACP v2.72 核心参考)

**状态**: 已确认
- 标题：「Bilateral signed interaction records as the trust primitive for A2A」
- 核心命题：一条双边签名记录 → 解决 identity/trust/audit/delegation/Sybil 五个问题
- ACP v2.72 `bilateral_ir_log` + v2.76 `bilateral_ir_merkle_root` 与之高度对齐

**新动向（本次可读内容）**:
- #1718 整合视角：#1628/trust scoring → #1716/delegation → #1712/WTRMRK → #1672/identity 需要一个统一原语
- ACP bilateral_ir 已是「统一原语」方向的领先实现

---

## v2.78 候选评估

| 候选 | 特性 | 依据 | 优先级 |
|------|------|------|--------|
| **A（首选）** | `PUT /trust/signals/capability-token` — SINT token 撤销/黑名单端点 | #1716 revocation 暗示（token_expired TOCTOU → 主动撤销） | ⭐⭐⭐ |
| **B** | `governance_metadata v2` — policy_compliance + audit_trail_reference 显式字段 | #1717 @imran-siddique | ⭐⭐ |
| **C** | `per-invocation trust scope` — invocation_context 扩展至 task 层 | #1628 @douglasborthwick-crypto | ⭐⭐ |
| **D** | `bilateral_ir signature verification` — 真实 Ed25519 签名验证（当前为 hmac mock） | #1718 @viftode4 Rust 实现参考 | ⭐⭐ |

**推荐 v2.78 = 候选 A**：`DELETE /trust/signals/capability-token/{jti}` 或 `POST /trust/signals/capability-token/revoke` — 主动撤销端点。理由：
1. v2.77 validate 实现后，自然下一步是 revoke（完整生命周期：issue→declare→fixture→validate→revoke）
2. #1716 SINT 协议原文提到 `exp` 约束 → 主动撤销是 exp 的补充
3. ACP SINT 四元组（v2.74+v2.75+v2.77+v2.78）形成完整闭环

---

## 回归状态确认

| 检查项 | 结论 |
|--------|------|
| #1716 @pshkv SINT PR 状态 | 待确认（截断），推断仍草稿 |
| ACP v2.77 对齐 #1716 | ✅ 完全对齐（SINT triad 完整） |
| #1717 @imran-siddique 是否新评论 | 无法确认（截断） |
| #1718 @viftode4 是否新评论 | 无法确认（截断） |
| ACP v2.77 无需回滚 | ✅ 确认，设计完全正确 |

---

## 结论

- ACP v2.77 SINT 三元组完全对齐 A2A #1716，是目前最完整的 SINT runtime enforcement 参考实现
- v2.78 首选：`POST /trust/signals/capability-token/revoke`（主动撤销，完整 token 生命周期）
- #1717 governance_metadata 仍是有效候选但优先级次于撤销端点
- 无 P0/P1 阻断，可进入下一开发轮

---

**生成时间**: 2026-04-07 20:42 CST
**下次轮转**: 测试轮（v2.77 完整回归 219+30 PASS 验证）或开发轮（v2.78 候选 A 撤销端点）
